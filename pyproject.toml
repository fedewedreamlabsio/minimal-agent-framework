[project]
name = "minimal-agent-framework"
version = "0.1.0"
description = "Hyper-minimal, replayable agent runtime"
requires-python = ">=3.10"

[project.scripts]
maf = "maf.cli:main"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
